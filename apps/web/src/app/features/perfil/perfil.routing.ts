import { Routes } from '@angular/router';
import { PerfilContainerComponent } from './container/perfil-container.component';

export const PERFIL_ROUTES: Routes = [{ path: '', component: PerfilContainerComponent }];
