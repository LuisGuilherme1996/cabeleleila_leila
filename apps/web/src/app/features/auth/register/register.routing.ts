import { Routes } from '@angular/router';
import { RegisterContainerComponent } from './container/register-container.component';

export const REGISTER_ROUTES: Routes = [{ path: '', component: RegisterContainerComponent }];
