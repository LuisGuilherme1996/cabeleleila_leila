import { Routes } from '@angular/router';
import { ResetPasswordContainerComponent } from './container/reset-password-container.component';

export const RESET_PASSWORD_ROUTES: Routes = [
  { path: '', component: ResetPasswordContainerComponent },
];
