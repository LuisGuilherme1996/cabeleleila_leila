import { Routes } from '@angular/router';
import { LoginContainerComponent } from './container/login-container.component';

export const LOGIN_ROUTES: Routes = [{ path: '', component: LoginContainerComponent }];
