import { Routes } from '@angular/router';
import { HomeContainerComponent } from './container/home-container.component';

export const HOME_ROUTES: Routes = [{ path: '', component: HomeContainerComponent }];
