import { Routes } from '@angular/router';
import { NotFoundContainerComponent } from './container/not-found-container.component';

export const NOT_FOUND_ROUTES: Routes = [{ path: '', component: NotFoundContainerComponent }];
