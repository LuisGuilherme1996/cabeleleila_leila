{
  "name": "cabeleleila-leila-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "description": "Monorepo for Cabeleleila Leila Scheduling & Management System",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "eslint-config": "*",
    "prettier": "^3.4.2",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
